# Market Risk Pro v10 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** v9.5 のコア判定機能はそのまま、3シナリオ並列・運用プロファイル(KV同期)・DCAバックテスト・3モードUI・急変Slackアラート・Claude+Gemini二重Briefingを追加した v10 を本番デプロイする。

**Architecture:** Next.js 16 App Router、Vercel Hobby、Vercel KV、Vercel Cron、CNN+Yahoo Finance自動取得、Claude haiku-4-5＋Gemini 3.1 Pro 並列、Slack Webhook、URL secret middleware。手入力UIは撤廃し自動取得値のみ。

**Tech Stack:** Next.js 16, React 19, Tailwind 4, TypeScript, Vercel KV, Vitest, @anthropic-ai/sdk, lucide-react

---

## Phase 0: テスト基盤と環境変数

### Task 0.1: Vitest 導入

**Files:**
- Modify: `package.json`
- Create: `vitest.config.ts`
- Create: `src/lib/__tests__/.gitkeep`

- [ ] **Step 1: 依存追加**

```bash
npm install -D vitest @vitest/ui
```

- [ ] **Step 2: package.json に scripts 追加**

`package.json` の `scripts` に追加：
```json
"test": "vitest run",
"test:watch": "vitest"
```

- [ ] **Step 3: vitest.config.ts 作成**

```ts
import { defineConfig } from 'vitest/config';
import path from 'path';

export default defineConfig({
  test: {
    environment: 'node',
    include: ['src/lib/**/*.test.ts'],
  },
  resolve: {
    alias: { '@': path.resolve(__dirname, './src') },
  },
});
```

- [ ] **Step 4: 動作確認**

```bash
mkdir -p src/lib/__tests__
touch src/lib/__tests__/.gitkeep
npm test
```
Expected: "No test files found" でゼロ件成功

- [ ] **Step 5: コミット**

```bash
git add package.json package-lock.json vitest.config.ts src/lib/__tests__/.gitkeep
git commit -m "chore: add vitest for pure logic testing"
```

### Task 0.2: 環境変数の準備

**Files:**
- Modify: `.env.local`
- Create: `.env.example`

- [ ] **Step 1: .env.local に追加（ローカル開発用、値はあとで入れる）**

既存の `.env.local` の末尾に追記：
```
ANTHROPIC_API_KEY=
MARKET_DB_SECRET_KEY=
MARKET_DB_SLACK_WEBHOOK=
KV_REST_API_URL=
KV_REST_API_TOKEN=
```

- [ ] **Step 2: .env.example 作成（チェックインする雛形）**

```
GEMINI_API_KEY=
ANTHROPIC_API_KEY=
MARKET_DB_SECRET_KEY=
MARKET_DB_SLACK_WEBHOOK=
KV_REST_API_URL=
KV_REST_API_TOKEN=
```

- [ ] **Step 3: コミット**

```bash
git add .env.example
git commit -m "chore: add env example for v10"
```

---

## Phase 1: page.tsx の責務分離

### Task 1.1: portfolioData を分離

**Files:**
- Create: `src/data/portfolio.ts`
- Modify: `src/app/page.tsx`

- [ ] **Step 1: portfolio.ts を作成**

`src/app/page.tsx` の `portfolioData` 配列（6行目から始まる）をまるごと切り出して `src/data/portfolio.ts` に移動。

```ts
export type Phase = "HEAT" | "PERFECT" | "HIGH" | "WATCH" | "NEUTRAL";

export type StrategyEntry = {
  budget: string;
  action: string;
};

export type PortfolioCategory = {
  id: string;
  category: string;
  items: string[];
  strategy: Record<Phase, StrategyEntry>;
};

export const portfolioData: PortfolioCategory[] = [
  // ... 既存の page.tsx から portfolioData の配列要素をすべてコピー
];
```

- [ ] **Step 2: page.tsx を更新**

`src/app/page.tsx` の冒頭 import に追加：
```ts
import { portfolioData } from '@/data/portfolio';
```

そして `const portfolioData = [...]` の定義部分（6行目〜123行目）を完全削除。

- [ ] **Step 3: 動作確認**

```bash
npm run dev
```
ブラウザで http://localhost:3000 を開き、ポートフォリオ表が崩れず表示されることを目視確認。

- [ ] **Step 4: コミット**

```bash
git add src/data/portfolio.ts src/app/page.tsx
git commit -m "refactor: extract portfolioData to src/data/portfolio.ts"
```

### Task 1.2: signals.ts を抽出（TDD）

**Files:**
- Create: `src/lib/signals.test.ts`
- Create: `src/lib/signals.ts`
- Modify: `src/app/page.tsx`

- [ ] **Step 1: 失敗テストを書く**

`src/lib/signals.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { detectPhase } from './signals';

describe('detectPhase', () => {
  it('HEAT when F&G >= 75', () => {
    expect(detectPhase({ fg: 80, vix: 15, skew: 130 })).toBe('HEAT');
  });
  it('PERFECT when all 3 BUY signals active', () => {
    expect(detectPhase({ fg: 20, vix: 30, skew: 110 })).toBe('PERFECT');
  });
  it('HIGH when 2 BUY signals active', () => {
    expect(detectPhase({ fg: 20, vix: 30, skew: 130 })).toBe('HIGH');
  });
  it('WATCH when 1 BUY signal active', () => {
    expect(detectPhase({ fg: 20, vix: 20, skew: 130 })).toBe('WATCH');
  });
  it('NEUTRAL when no BUY signals and F&G < 75', () => {
    expect(detectPhase({ fg: 50, vix: 20, skew: 130 })).toBe('NEUTRAL');
  });
  it('境界: F&G=30 は BUY シグナル on', () => {
    expect(detectPhase({ fg: 30, vix: 20, skew: 130 })).toBe('WATCH');
  });
  it('境界: VIX=28 は BUY シグナル on', () => {
    expect(detectPhase({ fg: 50, vix: 28, skew: 130 })).toBe('WATCH');
  });
  it('境界: Skew=118 は BUY シグナル on', () => {
    expect(detectPhase({ fg: 50, vix: 20, skew: 118 })).toBe('WATCH');
  });
});
```

- [ ] **Step 2: テスト実行（FAIL確認）**

```bash
npm test
```
Expected: "Cannot find module './signals'" でFAIL

- [ ] **Step 3: signals.ts を実装**

`src/lib/signals.ts`:
```ts
import type { Phase } from '@/data/portfolio';

export type Indicators = {
  fg: number;
  vix: number;
  skew: number;
};

export type Signal = { name: string; active: boolean };

export function buySignals(ind: Indicators): Signal[] {
  return [
    { name: 'F&G', active: ind.fg <= 30 },
    { name: 'VIX', active: ind.vix >= 28 },
    { name: 'Skew', active: ind.skew <= 118 },
  ];
}

export function detectPhase(ind: Indicators): Phase {
  if (ind.fg >= 75) return 'HEAT';
  const active = buySignals(ind).filter((s) => s.active).length;
  if (active === 3) return 'PERFECT';
  if (active === 2) return 'HIGH';
  if (active === 1) return 'WATCH';
  return 'NEUTRAL';
}

export type { Phase };
```

- [ ] **Step 4: テスト実行（PASS確認）**

```bash
npm test
```
Expected: 8 tests passed

- [ ] **Step 5: page.tsx の判定ロジックを置換**

`src/app/page.tsx` の `handleAnalyze` 内の判定ブロック（buySignals/isHeat/actCount/newStatus を計算する部分、約184-204行目）を以下に置換：

```ts
import { detectPhase } from '@/lib/signals';
// ...
const newStatus = detectPhase({
  fg: parseFloat(data.fg),
  vix: parseFloat(data.vix),
  skew: parseFloat(data.skew),
});
setStatus(newStatus);
```

- [ ] **Step 6: 動作確認**

```bash
npm run dev
```
ブラウザで指標値を入れて「AI分析開始」 → 既存と同じ判定が出ること。

- [ ] **Step 7: コミット**

```bash
git add src/lib/signals.ts src/lib/signals.test.ts src/app/page.tsx
git commit -m "refactor: extract detectPhase to src/lib/signals.ts with tests"
```

### Task 1.3: 手入力UIを撤廃

**Files:**
- Modify: `src/app/page.tsx`

- [ ] **Step 1: 手入力フォーム3個を「自動取得値の大型表示」に置換**

`src/app/page.tsx` の UNIFIED INPUT AREA ブロック（約270-296行目）を以下に置換：

```tsx
{/* AUTO-FETCHED INDICATORS */}
<div className="space-y-6">
  <div className="grid grid-cols-1 md:grid-cols-3 gap-6">
    <div className="glass-card rounded-3xl p-8 flex flex-col items-center justify-center space-y-4">
      <h2 className="text-xs font-black text-slate-400 uppercase tracking-widest flex items-center gap-2"><Gauge className="w-4 h-4" /> Fear & Greed</h2>
      <div className="text-6xl font-black text-white py-6">{data.fg || '--'}</div>
      <p className="text-[9px] text-slate-500 font-bold uppercase tracking-tighter">CNN Auto-fetch</p>
    </div>
    <div className="glass-card rounded-3xl p-8 flex flex-col items-center justify-center space-y-4">
      <h2 className="text-xs font-black text-slate-400 uppercase tracking-widest flex items-center gap-2"><Flame className="w-4 h-4" /> VIX Index</h2>
      <div className="text-6xl font-black text-white py-6">{data.vix || '--.--'}</div>
      <p className="text-[9px] text-slate-500 font-bold uppercase tracking-tighter">Yahoo Finance Auto-fetch</p>
    </div>
    <div className="glass-card rounded-3xl p-8 flex flex-col items-center justify-center space-y-4">
      <h2 className="text-xs font-black text-slate-400 uppercase tracking-widest flex items-center gap-2"><TrendingDown className="w-4 h-4" /> Skew Index</h2>
      <div className="text-6xl font-black text-white py-6">{data.skew || '---'}</div>
      <p className="text-[9px] text-slate-500 font-bold uppercase tracking-tighter">Yahoo Finance Auto-fetch</p>
    </div>
  </div>
</div>
```

- [ ] **Step 2: useEffect 内で取得完了後すぐに detectPhase を呼ぶよう変更**

`src/app/page.tsx` の `useEffect` 内、`setStrategyMessage(...)` の直後に追加：

```ts
const fgN = parseFloat(json.fg);
const vixN = parseFloat(json.vix);
const skewN = parseFloat(json.skew);
if (!Number.isNaN(fgN) && !Number.isNaN(vixN) && !Number.isNaN(skewN)) {
  setStatus(detectPhase({ fg: fgN, vix: vixN, skew: skewN }));
}
```

そして「この数値で確定・AI分析開始」ボタンと `handleAnalyze` 関数の入力値チェック（約176-180行目の if ブロック）を削除。`handleAnalyze` は AI Briefing 取得専用に縮小、ボタンラベルも「AI Briefing を更新」に変更。

- [ ] **Step 3: 動作確認**

```bash
npm run dev
```
ページを開いたら指標が自動で表示され、フェーズも自動判定されることを目視確認。

- [ ] **Step 4: コミット**

```bash
git add src/app/page.tsx
git commit -m "feat: remove manual input UI, show auto-fetched indicators directly"
```

---

## Phase 2: 3シナリオ並列カード

### Task 2.1: scenarios.ts を実装（TDD）

**Files:**
- Create: `src/lib/scenarios.test.ts`
- Create: `src/lib/scenarios.ts`

- [ ] **Step 1: 失敗テストを書く**

`src/lib/scenarios.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { neighborPhases } from './scenarios';

describe('neighborPhases', () => {
  it('HIGH の隣は PERFECT(悪化) と WATCH(改善)', () => {
    expect(neighborPhases('HIGH')).toEqual({ worse: 'PERFECT', better: 'WATCH' });
  });
  it('HEAT は悪化方向なし', () => {
    expect(neighborPhases('HEAT')).toEqual({ worse: null, better: 'PERFECT' });
  });
  it('NEUTRAL は改善方向なし', () => {
    expect(neighborPhases('NEUTRAL')).toEqual({ worse: 'WATCH', better: null });
  });
  it('PERFECT の隣は HEAT(悪化) と HIGH(改善)', () => {
    expect(neighborPhases('PERFECT')).toEqual({ worse: 'HEAT', better: 'HIGH' });
  });
  it('WATCH の隣は HIGH(悪化) と NEUTRAL(改善)', () => {
    expect(neighborPhases('WATCH')).toEqual({ worse: 'HIGH', better: 'NEUTRAL' });
  });
});
```

- [ ] **Step 2: テスト実行（FAIL確認）**

```bash
npm test
```

- [ ] **Step 3: scenarios.ts を実装**

`src/lib/scenarios.ts`:
```ts
import type { Phase } from './signals';

const order: Phase[] = ['HEAT', 'PERFECT', 'HIGH', 'WATCH', 'NEUTRAL'];

export function neighborPhases(current: Phase): { worse: Phase | null; better: Phase | null } {
  const idx = order.indexOf(current);
  return {
    worse: idx > 0 ? order[idx - 1] : null,
    better: idx < order.length - 1 ? order[idx + 1] : null,
  };
}
```

- [ ] **Step 4: テスト実行（PASS確認）**

```bash
npm test
```
Expected: 13 tests passed (5 new + 8 from signals)

- [ ] **Step 5: コミット**

```bash
git add src/lib/scenarios.ts src/lib/scenarios.test.ts
git commit -m "feat: add neighborPhases for 3-scenario projection"
```

### Task 2.2: 3シナリオカードを UI に追加

**Files:**
- Create: `src/components/ScenarioCards.tsx`
- Modify: `src/app/page.tsx`

- [ ] **Step 1: ScenarioCards コンポーネントを作成**

`src/components/ScenarioCards.tsx`:
```tsx
"use client";

import { neighborPhases } from '@/lib/scenarios';
import type { Phase } from '@/lib/signals';
import { portfolioData } from '@/data/portfolio';

const labels: Record<Phase, { text: string; color: string }> = {
  HEAT: { text: '利益確定', color: 'bg-red-600' },
  PERFECT: { text: '全力買付', color: 'bg-emerald-600' },
  HIGH: { text: '強気スポット', color: 'bg-emerald-500' },
  WATCH: { text: '打診買い', color: 'bg-indigo-500' },
  NEUTRAL: { text: '静観', color: 'bg-slate-600' },
};

function PhaseCard({ phase, role }: { phase: Phase; role: '1段悪化' | '現状' | '1段改善' }) {
  const label = labels[phase];
  const totalBudget = portfolioData.reduce((sum, cat) => {
    const b = cat.strategy[phase]?.budget?.match(/(\d+)万円/);
    return sum + (b ? parseInt(b[1], 10) : 0);
  }, 0);

  return (
    <div className={`glass-card rounded-2xl p-6 border-t-4 ${role === '現状' ? 'border-t-indigo-400 ring-2 ring-indigo-500/30' : 'border-t-slate-700'}`}>
      <div className="text-[10px] text-slate-500 uppercase tracking-widest font-black mb-2">{role}</div>
      <div className={`inline-block ${label.color} text-white text-xs font-black uppercase tracking-wider px-3 py-1 rounded mb-3`}>{phase}</div>
      <div className="text-lg font-black text-slate-200 mb-2">{label.text}</div>
      <div className="text-[10px] text-slate-500 uppercase font-bold">月配分合計</div>
      <div className="text-2xl font-mono font-black text-emerald-400">{totalBudget}万円</div>
    </div>
  );
}

export function ScenarioCards({ current }: { current: Phase }) {
  const { worse, better } = neighborPhases(current);
  return (
    <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
      {worse && <PhaseCard phase={worse} role="1段悪化" />}
      <PhaseCard phase={current} role="現状" />
      {better && <PhaseCard phase={better} role="1段改善" />}
    </div>
  );
}
```

- [ ] **Step 2: page.tsx に組み込み**

`src/app/page.tsx` の STRATEGY SECTION（約298-314行目）の直後に追加：

```tsx
import { ScenarioCards } from '@/components/ScenarioCards';
// ...
{/* SCENARIO PROJECTION */}
<div className="mt-12">
  <h2 className="text-sm font-black text-indigo-400 uppercase tracking-[0.3em] pl-2 mb-4">Scenario Projection</h2>
  <ScenarioCards current={status as any} />
</div>
```

- [ ] **Step 3: 動作確認**

ブラウザでフェーズに応じて3カードが並ぶこと、各カードに月配分合計が表示されることを目視確認。

- [ ] **Step 4: コミット**

```bash
git add src/components/ScenarioCards.tsx src/app/page.tsx
git commit -m "feat: add 3-scenario projection cards"
```

---

## Phase 3: Vercel KV ＋ プロファイル

### Task 3.1: Vercel KV セットアップ

**Files:**
- Modify: `package.json`

- [ ] **Step 1: @vercel/kv 依存追加**

```bash
npm install @vercel/kv
```

- [ ] **Step 2: Vercel ダッシュボードで KV 作成**

手順:
1. Vercel ダッシュボード → market-db プロジェクト → Storage タブ
2. Create Database → KV → 名前 `market-db-kv` でリージョン Asia Northeast
3. Connect to Project → Environment: All
4. Settings → Environment Variables から `KV_REST_API_URL` と `KV_REST_API_TOKEN` をコピー
5. `.env.local` に貼り付け

- [ ] **Step 3: 接続確認スクリプト**

```bash
node -e "require('@vercel/kv').kv.set('test', 'hello').then(()=>require('@vercel/kv').kv.get('test')).then(console.log)"
```
Expected: `hello`

- [ ] **Step 4: コミット**

```bash
git add package.json package-lock.json
git commit -m "chore: add @vercel/kv for profile storage"
```

### Task 3.2: profile.ts と /api/profile

**Files:**
- Create: `src/lib/profile.ts`
- Create: `src/app/api/profile/route.ts`

- [ ] **Step 1: profile.ts**

`src/lib/profile.ts`:
```ts
import { kv } from '@vercel/kv';

export type Profile = {
  cashPool: number;
  monthlyBudget: number;
  maxSingleAsset: number;
  maxDrawdownPct: number;
  adoptedScenario: 'worse' | 'current' | 'better';
};

const DEFAULT: Profile = {
  cashPool: 0,
  monthlyBudget: 500000,
  maxSingleAsset: 200000,
  maxDrawdownPct: 30,
  adoptedScenario: 'current',
};

const KEY = 'profile:satoru';

export async function getProfile(): Promise<Profile> {
  const stored = await kv.get<Profile>(KEY);
  return { ...DEFAULT, ...(stored ?? {}) };
}

export async function setProfile(patch: Partial<Profile>): Promise<Profile> {
  const current = await getProfile();
  const next = { ...current, ...patch };
  await kv.set(KEY, next);
  return next;
}
```

- [ ] **Step 2: /api/profile/route.ts**

`src/app/api/profile/route.ts`:
```ts
import { NextResponse } from 'next/server';
import { getProfile, setProfile, type Profile } from '@/lib/profile';

export async function GET() {
  return NextResponse.json(await getProfile());
}

export async function PATCH(request: Request) {
  const body = (await request.json()) as Partial<Profile>;
  return NextResponse.json(await setProfile(body));
}
```

- [ ] **Step 3: 動作確認**

```bash
npm run dev
curl http://localhost:3000/api/profile
curl -X PATCH http://localhost:3000/api/profile -H "Content-Type: application/json" -d '{"cashPool":8000000}'
curl http://localhost:3000/api/profile
```
Expected: cashPool が 8000000 で永続化されている

- [ ] **Step 4: コミット**

```bash
git add src/lib/profile.ts src/app/api/profile/route.ts
git commit -m "feat: add profile storage with Vercel KV"
```

### Task 3.3: プロファイル設定UI（フッターに折りたたみ）

**Files:**
- Create: `src/components/ProfilePanel.tsx`
- Modify: `src/app/page.tsx`

- [ ] **Step 1: ProfilePanel コンポーネント**

`src/components/ProfilePanel.tsx`:
```tsx
"use client";
import { useEffect, useState } from 'react';
import type { Profile } from '@/lib/profile';

export function ProfilePanel() {
  const [open, setOpen] = useState(false);
  const [profile, setProfile] = useState<Profile | null>(null);

  useEffect(() => {
    fetch('/api/profile').then(r => r.json()).then(setProfile);
  }, []);

  const save = async (patch: Partial<Profile>) => {
    const next = await fetch('/api/profile', {
      method: 'PATCH',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(patch),
    }).then(r => r.json());
    setProfile(next);
  };

  if (!profile) return null;

  return (
    <div className="max-w-7xl mx-auto mt-12">
      <button onClick={() => setOpen(!open)} className="text-xs text-slate-500 uppercase font-black tracking-widest">
        {open ? '▲ プロファイルを閉じる' : '▼ 運用プロファイルを編集'}
      </button>
      {open && (
        <div className="glass-card rounded-2xl p-6 mt-4 grid grid-cols-1 md:grid-cols-2 gap-4">
          {(['cashPool', 'monthlyBudget', 'maxSingleAsset', 'maxDrawdownPct'] as const).map(key => (
            <label key={key} className="block">
              <span className="text-[10px] text-slate-500 uppercase font-black tracking-widest block mb-1">{key}</span>
              <input
                type="number"
                value={profile[key]}
                onChange={e => save({ [key]: Number(e.target.value) } as any)}
                className="w-full bg-black/40 border border-slate-700 rounded px-3 py-2 text-white font-mono"
              />
            </label>
          ))}
        </div>
      )}
    </div>
  );
}
```

- [ ] **Step 2: page.tsx の末尾に追加**

`src/app/page.tsx` の HISTORICAL TACTICAL DATA ブロックの直後、最外 `<div>` の閉じる直前に：

```tsx
import { ProfilePanel } from '@/components/ProfilePanel';
// ...
<ProfilePanel />
```

- [ ] **Step 3: 動作確認**

ブラウザでフッターのリンクをクリック → パネル展開 → 値を変更 → リロードしても残ること。

- [ ] **Step 4: コミット**

```bash
git add src/components/ProfilePanel.tsx src/app/page.tsx
git commit -m "feat: add ProfilePanel UI with KV sync"
```

---

## Phase 4: DCAバックテスト

### Task 4.1: historical.json を整備

**Files:**
- Create: `public/data/historical.json`

- [ ] **Step 1: 過去イベントデータを作成**

`public/data/historical.json`:
```json
{
  "events": [
    { "date": "2024-08-05", "intensity": "PERFECT", "fg": 17, "vix_peak": 38.5, "skew_peak": 145, "context": "植田ショック" },
    { "date": "2024-04-19", "intensity": "HIGH", "fg": 31, "vix_peak": 21.2, "skew_peak": 138, "context": "テック株調整" },
    { "date": "2022-10-13", "intensity": "PERFECT", "fg": 15, "vix_peak": 33.8, "skew_peak": 119, "context": "CPIパニック" },
    { "date": "2022-06-17", "intensity": "PERFECT", "fg": 6, "vix_peak": 34.0, "skew_peak": 117, "context": "FRB75bp利上げ" },
    { "date": "2022-01-24", "intensity": "HIGH", "fg": 22, "vix_peak": 38.9, "skew_peak": 120, "context": "テック株急落" },
    { "date": "2020-03-23", "intensity": "PERFECT", "fg": 2, "vix_peak": 82.7, "skew_peak": 110, "context": "コロナショック" },
    { "date": "2018-12-24", "intensity": "PERFECT", "fg": 5, "vix_peak": 36.1, "skew_peak": 115, "context": "クリスマスイブ底" },
    { "date": "2018-02-08", "intensity": "HIGH", "fg": 12, "vix_peak": 50.3, "skew_peak": 130, "context": "VIXショック" },
    { "date": "2016-02-11", "intensity": "PERFECT", "fg": 10, "vix_peak": 28.1, "skew_peak": 115, "context": "原油安・中国懸念" },
    { "date": "2015-08-25", "intensity": "PERFECT", "fg": 8, "vix_peak": 40.7, "skew_peak": 119, "context": "チャイナショック" }
  ],
  "current_prices": {
    "SPXL": 175.0,
    "TQQQ": 92.0,
    "QQQ": 540.0,
    "VTI": 305.0,
    "VT": 130.0,
    "GLDM": 78.0,
    "IAU": 79.0,
    "VYM": 138.0,
    "TLT": 88.0
  },
  "event_prices": {
    "2024-08-05": { "SPXL": 105.0, "TQQQ": 50.0, "QQQ": 425.0, "VTI": 250.0, "VT": 105.0, "GLDM": 70.0, "IAU": 72.0, "VYM": 120.0, "TLT": 92.0 },
    "2024-04-19": { "SPXL": 115.0, "TQQQ": 56.0, "QQQ": 440.0, "VTI": 252.0, "VT": 107.0, "GLDM": 65.0, "IAU": 67.0, "VYM": 117.0, "TLT": 89.0 },
    "2022-10-13": { "SPXL": 50.0, "TQQQ": 18.0, "QQQ": 260.0, "VTI": 180.0, "VT": 82.0, "GLDM": 52.0, "IAU": 54.0, "VYM": 100.0, "TLT": 95.0 },
    "2022-06-17": { "SPXL": 55.0, "TQQQ": 22.0, "QQQ": 270.0, "VTI": 185.0, "VT": 86.0, "GLDM": 55.0, "IAU": 57.0, "VYM": 102.0, "TLT": 110.0 },
    "2022-01-24": { "SPXL": 110.0, "TQQQ": 60.0, "QQQ": 350.0, "VTI": 230.0, "VT": 100.0, "GLDM": 53.0, "IAU": 55.0, "VYM": 110.0, "TLT": 140.0 },
    "2020-03-23": { "SPXL": 15.0, "TQQQ": 5.0, "QQQ": 175.0, "VTI": 110.0, "VT": 60.0, "GLDM": 47.0, "IAU": 49.0, "VYM": 60.0, "TLT": 165.0 },
    "2018-12-24": { "SPXL": 24.0, "TQQQ": 10.0, "QQQ": 145.0, "VTI": 115.0, "VT": 60.0, "GLDM": 24.0, "IAU": 25.0, "VYM": 75.0, "TLT": 121.0 },
    "2018-02-08": { "SPXL": 38.0, "TQQQ": 18.0, "QQQ": 155.0, "VTI": 130.0, "VT": 70.0, "GLDM": 25.0, "IAU": 26.0, "VYM": 80.0, "TLT": 122.0 },
    "2016-02-11": { "SPXL": 22.0, "TQQQ": 8.0, "QQQ": 95.0, "VTI": 95.0, "VT": 53.0, "GLDM": 25.0, "IAU": 26.0, "VYM": 65.0, "TLT": 132.0 },
    "2015-08-25": { "SPXL": 35.0, "TQQQ": 14.0, "QQQ": 105.0, "VTI": 100.0, "VT": 58.0, "GLDM": 22.0, "IAU": 22.5, "VYM": 70.0, "TLT": 122.0 }
  }
}
```

注：価格は概算。実運用前に Yahoo Finance 等で正確値に置き換える。

- [ ] **Step 2: コミット**

```bash
git add public/data/historical.json
git commit -m "feat: add historical events and prices for backtest"
```

### Task 4.2: backtest.ts を実装（TDD）

**Files:**
- Create: `src/lib/backtest.test.ts`
- Create: `src/lib/backtest.ts`

- [ ] **Step 1: 失敗テストを書く**

`src/lib/backtest.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { computeReturns } from './backtest';

const sample = {
  events: [
    { date: '2020-03-23', intensity: 'PERFECT' as const, fg: 2, vix_peak: 82, skew_peak: 110, context: 'A' },
    { date: '2022-10-13', intensity: 'PERFECT' as const, fg: 15, vix_peak: 33, skew_peak: 119, context: 'B' },
  ],
  current_prices: { SPXL: 100 },
  event_prices: {
    '2020-03-23': { SPXL: 10 },
    '2022-10-13': { SPXL: 50 },
  },
};

describe('computeReturns', () => {
  it('累積リターンを正しく計算', () => {
    const result = computeReturns(sample, 'SPXL');
    expect(result.byEvent[0].returnPct).toBeCloseTo(9.0); // 10→100 = +900%
    expect(result.byEvent[1].returnPct).toBeCloseTo(1.0); // 50→100 = +100%
  });

  it('平均リターンと勝率を計算', () => {
    const result = computeReturns(sample, 'SPXL');
    expect(result.avgReturnPct).toBeCloseTo(5.0); // (9 + 1) / 2
    expect(result.winRate).toBe(1.0); // 全勝
  });

  it('価格データなしの資産は空結果', () => {
    const result = computeReturns(sample, 'BTC');
    expect(result.byEvent).toEqual([]);
    expect(result.avgReturnPct).toBe(0);
  });
});
```

- [ ] **Step 2: テスト実行（FAIL確認）**

```bash
npm test
```

- [ ] **Step 3: backtest.ts を実装**

`src/lib/backtest.ts`:
```ts
export type HistEvent = {
  date: string;
  intensity: 'PERFECT' | 'HIGH' | 'WATCH';
  fg: number;
  vix_peak: number;
  skew_peak: number;
  context: string;
};

export type HistoricalData = {
  events: HistEvent[];
  current_prices: Record<string, number>;
  event_prices: Record<string, Record<string, number>>;
};

export type EventReturn = {
  date: string;
  context: string;
  intensity: HistEvent['intensity'];
  returnPct: number;
};

export type BacktestResult = {
  ticker: string;
  byEvent: EventReturn[];
  avgReturnPct: number;
  winRate: number;
};

export function computeReturns(data: HistoricalData, ticker: string): BacktestResult {
  const current = data.current_prices[ticker];
  if (current === undefined) return { ticker, byEvent: [], avgReturnPct: 0, winRate: 0 };

  const byEvent: EventReturn[] = data.events
    .map((ev) => {
      const price = data.event_prices[ev.date]?.[ticker];
      if (price === undefined) return null;
      return {
        date: ev.date,
        context: ev.context,
        intensity: ev.intensity,
        returnPct: (current - price) / price,
      };
    })
    .filter((x): x is EventReturn => x !== null);

  if (byEvent.length === 0) return { ticker, byEvent: [], avgReturnPct: 0, winRate: 0 };

  const avg = byEvent.reduce((s, e) => s + e.returnPct, 0) / byEvent.length;
  const winRate = byEvent.filter((e) => e.returnPct > 0).length / byEvent.length;

  return { ticker, byEvent, avgReturnPct: avg, winRate };
}
```

- [ ] **Step 4: テスト実行（PASS確認）**

```bash
npm test
```

- [ ] **Step 5: コミット**

```bash
git add src/lib/backtest.ts src/lib/backtest.test.ts
git commit -m "feat: add DCA backtest computation"
```

### Task 4.3: バックテストUI

**Files:**
- Create: `src/app/api/backtest/route.ts`
- Create: `src/components/BacktestPanel.tsx`
- Modify: `src/app/page.tsx`

- [ ] **Step 1: /api/backtest/route.ts**

```ts
import { NextResponse } from 'next/server';
import { promises as fs } from 'fs';
import path from 'path';
import { computeReturns, type HistoricalData } from '@/lib/backtest';

const TICKERS = ['SPXL', 'TQQQ', 'QQQ', 'VTI', 'VT', 'GLDM', 'VYM', 'TLT'];

export async function GET() {
  const filePath = path.join(process.cwd(), 'public/data/historical.json');
  const raw = await fs.readFile(filePath, 'utf-8');
  const data = JSON.parse(raw) as HistoricalData;
  const results = TICKERS.map((t) => computeReturns(data, t));
  return NextResponse.json(results);
}
```

- [ ] **Step 2: BacktestPanel コンポーネント**

`src/components/BacktestPanel.tsx`:
```tsx
"use client";
import { useEffect, useState } from 'react';
import type { BacktestResult } from '@/lib/backtest';

export function BacktestPanel() {
  const [results, setResults] = useState<BacktestResult[]>([]);

  useEffect(() => {
    fetch('/api/backtest').then(r => r.json()).then(setResults);
  }, []);

  return (
    <div className="max-w-7xl mx-auto mt-12 pb-12">
      <h2 className="text-sm font-black text-emerald-400 uppercase tracking-[0.3em] pl-2 mb-4">
        DCA Backtest (発火日に買付 → 現在のリターン)
      </h2>
      <div className="glass-card rounded-3xl p-6 overflow-x-auto">
        <table className="w-full text-left text-sm">
          <thead className="text-[10px] text-slate-500 uppercase font-black tracking-widest">
            <tr>
              <th className="p-3">Ticker</th>
              <th className="p-3 text-right">平均リターン</th>
              <th className="p-3 text-right">勝率</th>
              <th className="p-3 text-right">サンプル数</th>
            </tr>
          </thead>
          <tbody>
            {results.map(r => (
              <tr key={r.ticker} className="border-t border-white/5">
                <td className="p-3 font-black text-indigo-300">{r.ticker}</td>
                <td className={`p-3 text-right font-mono font-bold ${r.avgReturnPct > 0 ? 'text-emerald-400' : 'text-red-400'}`}>
                  {(r.avgReturnPct * 100).toFixed(1)}%
                </td>
                <td className="p-3 text-right font-mono">{(r.winRate * 100).toFixed(0)}%</td>
                <td className="p-3 text-right font-mono text-slate-500">{r.byEvent.length}</td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>
    </div>
  );
}
```

- [ ] **Step 3: page.tsx に組み込み**

`src/app/page.tsx` の HISTORICAL TACTICAL DATA の前に：

```tsx
import { BacktestPanel } from '@/components/BacktestPanel';
// ...
<BacktestPanel />
```

- [ ] **Step 4: 動作確認**

ブラウザで Backtest テーブルが表示され、リターン・勝率が出ること。

- [ ] **Step 5: コミット**

```bash
git add src/app/api/backtest/route.ts src/components/BacktestPanel.tsx src/app/page.tsx
git commit -m "feat: add DCA backtest panel"
```

---

## Phase 5: Quick Glance（スマホ専用）

### Task 5.1: /glance ページ

**Files:**
- Create: `src/app/glance/page.tsx`

- [ ] **Step 1: ページ作成**

```tsx
"use client";
import { useEffect, useState } from 'react';
import { detectPhase, type Phase } from '@/lib/signals';

const labels: Record<Phase, { text: string; color: string }> = {
  HEAT: { text: '利益確定', color: 'bg-red-600' },
  PERFECT: { text: '全力買付', color: 'bg-emerald-600' },
  HIGH: { text: '強気スポット', color: 'bg-emerald-500' },
  WATCH: { text: '打診買い', color: 'bg-indigo-500' },
  NEUTRAL: { text: '静観', color: 'bg-slate-600' },
};

export default function GlancePage() {
  const [data, setData] = useState<any>(null);
  const [phase, setPhase] = useState<Phase>('NEUTRAL');

  useEffect(() => {
    fetch('/api/market-data').then(r => r.json()).then(d => {
      setData(d);
      if (typeof d.fg === 'number' && typeof d.vix === 'number' && typeof d.skew === 'number') {
        setPhase(detectPhase({ fg: d.fg, vix: d.vix, skew: d.skew }));
      }
    });
  }, []);

  const label = labels[phase];

  return (
    <div className="min-h-screen p-4 flex flex-col gap-4 text-slate-100">
      <div className={`${label.color} rounded-2xl p-8 text-center`}>
        <div className="text-xs font-black uppercase tracking-widest opacity-80">{phase}</div>
        <div className="text-4xl font-black mt-2">{label.text}</div>
      </div>
      {data && (
        <div className="glass-card rounded-2xl p-6 space-y-2 font-mono">
          <div className="flex justify-between"><span className="text-slate-500">F&G</span><span className="font-black text-xl">{data.fg ?? '--'}</span></div>
          <div className="flex justify-between"><span className="text-slate-500">VIX</span><span className="font-black text-xl">{data.vix ?? '--'}</span></div>
          <div className="flex justify-between"><span className="text-slate-500">Skew</span><span className="font-black text-xl">{data.skew ?? '--'}</span></div>
          <div className="flex justify-between"><span className="text-slate-500">10Y</span><span className="font-black text-xl">{data.yield ?? '--'}</span></div>
        </div>
      )}
      <a href="/" className="text-center text-xs text-slate-500 uppercase font-black tracking-widest py-4">▼ Daily Briefing 詳細を見る</a>
    </div>
  );
}
```

- [ ] **Step 2: 動作確認**

ブラウザで http://localhost:3000/glance を開き、スマホサイズでも崩れないか DevTools のレスポンシブモードで確認。

- [ ] **Step 3: コミット**

```bash
git add src/app/glance/page.tsx
git commit -m "feat: add Quick Glance mobile page"
```

---

## Phase 6: Claude Briefing ＋ タブ切替

### Task 6.1: Claude API ルート

**Files:**
- Modify: `package.json`
- Create: `src/app/api/insight/claude/route.ts`
- Rename: `src/app/api/alpha-insight/route.ts` → `src/app/api/insight/gemini/route.ts`

- [ ] **Step 1: 依存追加**

```bash
npm install @anthropic-ai/sdk
```

- [ ] **Step 2: Claude エンドポイント**

`src/app/api/insight/claude/route.ts`:
```ts
import { NextResponse } from 'next/server';
import Anthropic from '@anthropic-ai/sdk';

const client = new Anthropic({ apiKey: process.env.ANTHROPIC_API_KEY });

export async function POST(request: Request) {
  const { fg, vix, skew } = await request.json();
  const prompt = `あなたは投資戦略アナリストです。F&G:${fg}, VIX:${vix}, Skew:${skew}。このデータに基づき、日本/米国株の投資家が今日取るべき戦術を150文字以内で精緻に分析してください。`;

  const msg = await client.messages.create({
    model: 'claude-haiku-4-5',
    max_tokens: 400,
    messages: [{ role: 'user', content: prompt }],
  });

  const text = msg.content
    .filter((b) => b.type === 'text')
    .map((b: any) => b.text)
    .join('');

  return NextResponse.json({ insight: text || 'Claude分析の取得に失敗しました。' });
}
```

- [ ] **Step 3: Gemini エンドポイントをリネーム**

```bash
mkdir -p src/app/api/insight/gemini
git mv src/app/api/alpha-insight/route.ts src/app/api/insight/gemini/route.ts
rmdir src/app/api/alpha-insight 2>/dev/null || true
```

- [ ] **Step 4: page.tsx の呼び出し先を更新**

`src/app/page.tsx` の `handleAnalyze` 内、fetch 先を `/api/alpha-insight` から `/api/insight/gemini` に変更。

- [ ] **Step 5: 動作確認**

```bash
curl -X POST http://localhost:3000/api/insight/claude -H "Content-Type: application/json" -d '{"fg":30,"vix":25,"skew":135}'
curl -X POST http://localhost:3000/api/insight/gemini -H "Content-Type: application/json" -d '{"fg":30,"vix":25,"skew":135}'
```
Expected: 両方とも insight フィールドに分析文。

- [ ] **Step 6: コミット**

```bash
git add package.json package-lock.json src/app/api/insight/ src/app/api/alpha-insight 2>/dev/null
git add -u
git commit -m "feat: add Claude insight endpoint, rename gemini path"
```

### Task 6.2: タブ切替UI

**Files:**
- Create: `src/components/InsightTabs.tsx`
- Modify: `src/app/page.tsx`

- [ ] **Step 1: InsightTabs コンポーネント**

`src/components/InsightTabs.tsx`:
```tsx
"use client";
import { useEffect, useState } from 'react';
import { Loader2, BrainCircuit } from 'lucide-react';

type Tab = 'claude' | 'gemini';

export function InsightTabs({ indicators }: { indicators: { fg: number; vix: number; skew: number } | null }) {
  const [active, setActive] = useState<Tab>('claude');
  const [texts, setTexts] = useState<Record<Tab, string>>({ claude: '', gemini: '' });
  const [loading, setLoading] = useState<Record<Tab, boolean>>({ claude: false, gemini: false });

  const fetchInsight = async (tab: Tab) => {
    if (!indicators) return;
    setLoading(l => ({ ...l, [tab]: true }));
    try {
      const res = await fetch(`/api/insight/${tab}`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(indicators),
      });
      const json = await res.json();
      setTexts(t => ({ ...t, [tab]: json.insight || 'エラー' }));
    } catch {
      setTexts(t => ({ ...t, [tab]: 'ネットワークエラー' }));
    } finally {
      setLoading(l => ({ ...l, [tab]: false }));
    }
  };

  useEffect(() => {
    if (indicators) {
      fetchInsight('claude');
      fetchInsight('gemini');
    }
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [indicators?.fg, indicators?.vix, indicators?.skew]);

  return (
    <div className="glass-card rounded-3xl p-8 border-l-8 border-l-indigo-600">
      <div className="flex items-center justify-between mb-4">
        <h2 className="text-sm font-black text-indigo-400 uppercase tracking-[0.3em] flex items-center gap-3">
          <BrainCircuit className="w-4 h-4" /> AI Briefing
        </h2>
        <div className="flex gap-2">
          {(['claude', 'gemini'] as Tab[]).map(t => (
            <button
              key={t}
              onClick={() => setActive(t)}
              className={`px-3 py-1 rounded text-xs font-black uppercase tracking-wider ${active === t ? 'bg-indigo-600 text-white' : 'bg-slate-800 text-slate-400'}`}
            >
              {t}
            </button>
          ))}
        </div>
      </div>
      <div className="text-base text-slate-200 font-bold italic leading-relaxed min-h-[80px]">
        {loading[active] ? (
          <div className="flex items-center gap-3 text-indigo-400">
            <Loader2 className="animate-spin w-5 h-5" />
            {active} 分析中...
          </div>
        ) : texts[active] || '指標取得待ち'}
      </div>
    </div>
  );
}
```

- [ ] **Step 2: page.tsx に組み込み**

`src/app/page.tsx` の AI Daily Alpha Insight ブロック（既存の `<h2>AI Daily Alpha Insight</h2>` のセクション全体）を以下に置換：

```tsx
import { InsightTabs } from '@/components/InsightTabs';
// ...
<InsightTabs indicators={data.fg && data.vix && data.skew ? {
  fg: parseFloat(data.fg as any),
  vix: parseFloat(data.vix as any),
  skew: parseFloat(data.skew as any),
} : null} />
```

そして既存の `isAnalyzing`, `setIsAnalyzing`, `aiInsight`, `setAiInsight`, `handleAnalyze` を削除（タブが自動 fetch するため）。「AI分析開始」ボタンも削除。

- [ ] **Step 3: 動作確認**

ブラウザで Claude/Gemini タブの切替、両方とも分析文が出ることを確認。

- [ ] **Step 4: コミット**

```bash
git add src/components/InsightTabs.tsx src/app/page.tsx
git commit -m "feat: add Claude+Gemini tabbed insight panel"
```

---

## Phase 7: Cron ＋ Slack アラート

### Task 7.1: アラート判定ロジック（TDD）

**Files:**
- Create: `src/lib/alerts.test.ts`
- Create: `src/lib/alerts.ts`

- [ ] **Step 1: 失敗テストを書く**

`src/lib/alerts.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { detectAlerts } from './alerts';

describe('detectAlerts', () => {
  it('VIX>30 で vix_spike', () => {
    const r = detectAlerts({ fg: 50, vix: 32, skew: 135 }, null);
    expect(r.some(a => a.key === 'vix_spike')).toBe(true);
  });
  it('F&G<20 で fg_panic', () => {
    const r = detectAlerts({ fg: 18, vix: 20, skew: 130 }, null);
    expect(r.some(a => a.key === 'fg_panic')).toBe(true);
  });
  it('フェーズ変化検知', () => {
    const r = detectAlerts({ fg: 18, vix: 30, skew: 117 }, 'NEUTRAL');
    expect(r.some(a => a.key === 'phase_change')).toBe(true);
  });
  it('変化なしなら空配列', () => {
    const r = detectAlerts({ fg: 50, vix: 18, skew: 135 }, 'NEUTRAL');
    expect(r).toEqual([]);
  });
});
```

- [ ] **Step 2: テスト実行（FAIL確認）**

```bash
npm test
```

- [ ] **Step 3: alerts.ts を実装**

`src/lib/alerts.ts`:
```ts
import { detectPhase, type Indicators, type Phase } from './signals';

export type Alert = {
  key: string;
  title: string;
  detail: string;
  phase: Phase;
};

export function detectAlerts(ind: Indicators, prevPhase: Phase | null): Alert[] {
  const phase = detectPhase(ind);
  const out: Alert[] = [];

  if (ind.vix > 30) {
    out.push({ key: 'vix_spike', title: 'VIX SPIKE', detail: `VIX: ${ind.vix}`, phase });
  }
  if (ind.fg < 20) {
    out.push({ key: 'fg_panic', title: 'F&G PANIC', detail: `F&G: ${ind.fg}`, phase });
  }
  if (ind.skew > 150) {
    out.push({ key: 'skew_high', title: 'SKEW HIGH', detail: `Skew: ${ind.skew}`, phase });
  }
  if (prevPhase && prevPhase !== phase) {
    out.push({ key: 'phase_change', title: `PHASE CHANGE: ${prevPhase} → ${phase}`, detail: `F&G:${ind.fg} VIX:${ind.vix} Skew:${ind.skew}`, phase });
  }

  return out;
}
```

- [ ] **Step 4: テスト実行（PASS確認）**

```bash
npm test
```

- [ ] **Step 5: コミット**

```bash
git add src/lib/alerts.ts src/lib/alerts.test.ts
git commit -m "feat: add alert detection logic"
```

### Task 7.2: Slack 通知エンドポイントと Cron

**Files:**
- Create: `src/app/api/cron/alert/route.ts`
- Create: `vercel.json`

- [ ] **Step 1: Slack 通知関数とCron API**

`src/app/api/cron/alert/route.ts`:
```ts
import { NextResponse } from 'next/server';
import { kv } from '@vercel/kv';
import { detectAlerts } from '@/lib/alerts';
import { detectPhase, type Phase } from '@/lib/signals';

const ALERT_LOCK_HOURS = 24;
const PHASE_KEY = 'snapshot:lastPhase';
const LASTFIRED_KEY = 'alert:lastFired';

async function sendSlack(text: string) {
  const url = process.env.MARKET_DB_SLACK_WEBHOOK;
  if (!url) return;
  await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ text }),
  });
}

export async function GET(request: Request) {
  // Vercel Cron は header に x-vercel-cron を付ける
  const authHeader = request.headers.get('authorization');
  if (process.env.CRON_SECRET && authHeader !== `Bearer ${process.env.CRON_SECRET}`) {
    return NextResponse.json({ error: 'unauthorized' }, { status: 401 });
  }

  const origin = new URL(request.url).origin;
  const md = await fetch(`${origin}/api/market-data`).then(r => r.json());

  const ind = {
    fg: Number(md.fg),
    vix: Number(md.vix),
    skew: Number(md.skew),
  };
  if (Number.isNaN(ind.fg) || Number.isNaN(ind.vix) || Number.isNaN(ind.skew)) {
    return NextResponse.json({ skipped: 'no indicators' });
  }

  const prevPhase = await kv.get<Phase>(PHASE_KEY);
  const alerts = detectAlerts(ind, prevPhase);
  const currentPhase = detectPhase(ind);
  await kv.set(PHASE_KEY, currentPhase);

  const lastFired = (await kv.get<Record<string, string>>(LASTFIRED_KEY)) ?? {};
  const now = Date.now();
  const fired: string[] = [];
  const secret = process.env.MARKET_DB_SECRET_KEY ?? '';

  for (const a of alerts) {
    const last = lastFired[a.key] ? new Date(lastFired[a.key]).getTime() : 0;
    if (now - last < ALERT_LOCK_HOURS * 60 * 60 * 1000) continue;
    const url = `${origin}/alert?event=${a.key}${secret ? `&key=${secret}` : ''}`;
    await sendSlack(`🚨 ${a.title}\n${a.detail}\n📊 ${url}`);
    lastFired[a.key] = new Date(now).toISOString();
    fired.push(a.key);
  }

  await kv.set(LASTFIRED_KEY, lastFired);
  return NextResponse.json({ phase: currentPhase, alerts: alerts.length, fired });
}
```

- [ ] **Step 2: vercel.json**

`vercel.json`:
```json
{
  "crons": [
    {
      "path": "/api/cron/alert",
      "schedule": "*/15 * * * *"
    }
  ]
}
```

- [ ] **Step 3: Slack チャンネル準備**

手順:
1. Slack で `#market-db-alerts` チャンネルを作成
2. https://api.slack.com/apps → Incoming Webhooks → New Webhook to Workspace
3. チャンネルに `#market-db-alerts` を選択 → Add
4. 払い出された URL を `.env.local` と Vercel 環境変数 `MARKET_DB_SLACK_WEBHOOK` に登録

- [ ] **Step 4: 手動発火テスト**

```bash
curl http://localhost:3000/api/cron/alert
```
Expected: `{"phase":"...","alerts":N,"fired":[...]}`、Slack に通知が来ること（指標値による）

- [ ] **Step 5: コミット**

```bash
git add src/app/api/cron/alert/route.ts vercel.json
git commit -m "feat: add Cron alert detection with Slack notification"
```

---

## Phase 8: Alert モード画面

### Task 8.1: /alert ページ

**Files:**
- Create: `src/app/alert/page.tsx`

- [ ] **Step 1: ページ作成**

```tsx
"use client";
import { useEffect, useState, Suspense } from 'react';
import { useSearchParams } from 'next/navigation';
import { detectPhase, type Phase } from '@/lib/signals';
import type { BacktestResult } from '@/lib/backtest';

const eventLabels: Record<string, string> = {
  vix_spike: 'VIX SPIKE (>30)',
  fg_panic: 'F&G PANIC (<20)',
  skew_high: 'SKEW HIGH (>150)',
  phase_change: 'PHASE CHANGE',
};

function AlertContent() {
  const params = useSearchParams();
  const event = params.get('event') ?? 'unknown';
  const [data, setData] = useState<any>(null);
  const [backtest, setBacktest] = useState<BacktestResult[]>([]);

  useEffect(() => {
    fetch('/api/market-data').then(r => r.json()).then(setData);
    fetch('/api/backtest').then(r => r.json()).then(setBacktest);
  }, []);

  const phase: Phase | null = data && typeof data.fg === 'number'
    ? detectPhase({ fg: data.fg, vix: data.vix, skew: data.skew })
    : null;

  return (
    <div className="min-h-screen p-6 space-y-6 text-slate-100">
      <div className="max-w-3xl mx-auto">
        <div className="text-xs text-red-400 uppercase font-black tracking-widest mb-2">🚨 ALERT FIRED</div>
        <h1 className="text-3xl font-black mb-1">{eventLabels[event] ?? event}</h1>
        <div className="text-sm text-slate-500">{new Date().toLocaleString('ja-JP')}</div>
      </div>

      {data && (
        <div className="max-w-3xl mx-auto glass-card rounded-2xl p-6">
          <div className="text-xs uppercase text-slate-500 font-black mb-3">現在指標</div>
          <div className="grid grid-cols-3 gap-4 font-mono">
            <div><div className="text-xs text-slate-500">F&G</div><div className="text-2xl font-black">{data.fg}</div></div>
            <div><div className="text-xs text-slate-500">VIX</div><div className="text-2xl font-black">{data.vix}</div></div>
            <div><div className="text-xs text-slate-500">Skew</div><div className="text-2xl font-black">{data.skew}</div></div>
          </div>
          {phase && <div className="mt-4 text-emerald-400 font-black uppercase tracking-wider">→ Phase: {phase}</div>}
        </div>
      )}

      {backtest.length > 0 && (
        <div className="max-w-3xl mx-auto glass-card rounded-2xl p-6">
          <div className="text-xs uppercase text-slate-500 font-black mb-3">過去発火後リターン（参考）</div>
          <div className="space-y-2 font-mono text-sm">
            {backtest.slice(0, 5).map(r => (
              <div key={r.ticker} className="flex justify-between">
                <span className="text-indigo-300 font-black">{r.ticker}</span>
                <span className={r.avgReturnPct > 0 ? 'text-emerald-400' : 'text-red-400'}>
                  Avg {(r.avgReturnPct * 100).toFixed(1)}% / 勝率 {(r.winRate * 100).toFixed(0)}%
                </span>
              </div>
            ))}
          </div>
        </div>
      )}

      <div className="max-w-3xl mx-auto text-center">
        <a href="/" className="text-sm text-slate-500 uppercase font-black tracking-widest">▼ Daily Briefing に戻る</a>
      </div>
    </div>
  );
}

export default function AlertPage() {
  return (
    <Suspense fallback={<div className="p-6 text-slate-400">Loading...</div>}>
      <AlertContent />
    </Suspense>
  );
}
```

- [ ] **Step 2: 動作確認**

ブラウザで http://localhost:3000/alert?event=vix_spike を開き、表示されることを確認。

- [ ] **Step 3: コミット**

```bash
git add src/app/alert/page.tsx
git commit -m "feat: add Alert mode page"
```

---

## Phase 9: secret key middleware

### Task 9.1: middleware 実装

**Files:**
- Create: `src/middleware.ts`

- [ ] **Step 1: middleware**

`src/middleware.ts`:
```ts
import { NextResponse, type NextRequest } from 'next/server';

const PUBLIC_PATHS = ['/api/cron/alert']; // Cron は別認証

export function middleware(request: NextRequest) {
  const { pathname, searchParams } = request.nextUrl;

  if (PUBLIC_PATHS.some(p => pathname.startsWith(p))) {
    return NextResponse.next();
  }

  const secret = process.env.MARKET_DB_SECRET_KEY;
  if (!secret) return NextResponse.next(); // 環境変数未設定なら無防備

  const cookieKey = request.cookies.get('mdb_key')?.value;
  const queryKey = searchParams.get('key');

  if (cookieKey === secret) return NextResponse.next();

  if (queryKey === secret) {
    const res = NextResponse.next();
    res.cookies.set('mdb_key', secret, {
      maxAge: 60 * 60 * 24 * 365,
      httpOnly: true,
      sameSite: 'lax',
      secure: true,
    });
    return res;
  }

  return new NextResponse('Forbidden', { status: 403 });
}

export const config = {
  matcher: [
    '/((?!_next/static|_next/image|favicon.ico|icon|apple-icon|manifest|.*\\.(?:jpg|jpeg|png|svg|ico|webmanifest)).*)',
  ],
};
```

- [ ] **Step 2: .env.local に secret を生成して入れる**

```bash
node -e "console.log(require('crypto').randomBytes(24).toString('hex'))"
```
出力を `.env.local` の `MARKET_DB_SECRET_KEY=` に貼る。Vercel 環境変数にも同じ値を登録。

- [ ] **Step 3: 動作確認**

```bash
npm run dev
```
- http://localhost:3000 → 403
- http://localhost:3000/?key=<生成したsecret> → 通る、以降 cookie で通る
- http://localhost:3000/glance → cookie あれば通る

- [ ] **Step 4: コミット**

```bash
git add src/middleware.ts
git commit -m "feat: add secret-key middleware for personal access"
```

---

## Phase 10: 本番デプロイと検証

### Task 10.1: Vercel デプロイ

- [ ] **Step 1: 環境変数を Vercel に登録**

Vercel ダッシュボード → Settings → Environment Variables で以下を全環境（Production/Preview/Development）に登録：
- `ANTHROPIC_API_KEY`
- `MARKET_DB_SECRET_KEY`
- `MARKET_DB_SLACK_WEBHOOK`
- `KV_REST_API_URL`（KV作成時に自動）
- `KV_REST_API_TOKEN`（KV作成時に自動）
- `GEMINI_API_KEY`（既存）

- [ ] **Step 2: push してデプロイ**

```bash
git push
```

Vercel ダッシュボードでビルド成功を確認。

- [ ] **Step 3: 本番動作確認チェックリスト**

ブックマーク URL: `https://market-db-five.vercel.app/?key=<secret>`

確認項目:
- [ ] PCブラウザで Daily Briefing が表示される
- [ ] 指標が自動取得される
- [ ] 3シナリオカードが表示される
- [ ] Claude/Gemini タブ両方で分析文が出る
- [ ] DCA Backtest テーブルが表示される
- [ ] ProfilePanel で値を変更 → リロードしても残る
- [ ] スマホで /glance を開いて崩れない
- [ ] /alert?event=vix_spike が表示される
- [ ] secret key なしの URL は 403
- [ ] Vercel ダッシュボードで Cron が15分毎に走っている
- [ ] 手動 `curl https://market-db-five.vercel.app/api/cron/alert?key=<secret>` で Slack 通知

- [ ] **Step 4: 最終コミット（必要なら微調整）**

```bash
git add -A
git commit -m "chore: v10 production deploy verified"
```

---

## 完了基準

- 全 Phase の commit が main にマージ済み
- 本番デプロイ後の動作確認チェックリスト全項目 OK
- Slack `#market-db-alerts` に少なくとも1回テスト通知が届く
- 朝・場中・急変の3シーンを実際に1回ずつ通して使ってみて違和感がない
